assemble_baseline {
    tasks = ["assemble"]
    cleanup-tasks = ["clean"]
    git-checkout = {
        build = "az/2.x/error-prone-baseline"
    }
}

assemble_error_prone {
    tasks = ["assemble"]
    cleanup-tasks = ["clean"]
    git-checkout = {
        build = "az/2.x/error-prone"
    }   
}

assemble_error_prone_disable_most_expensive_checks {
    tasks = ["assemble"]
    cleanup-tasks = ["clean"]
    git-checkout = {
        build = "az/2.x/error-prone-disable-longest-checks"
    }   
}
